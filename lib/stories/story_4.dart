import 'package:flutter/material.dart';

class Story_4 extends StatelessWidget {
  const Story_4({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      backgroundColor: Colors.pinkAccent,
    );
  }
}
