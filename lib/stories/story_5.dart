import 'package:flutter/material.dart';

class Story_5 extends StatelessWidget {
  const Story_5({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      backgroundColor: Colors.deepOrange,
    );
  }
}
