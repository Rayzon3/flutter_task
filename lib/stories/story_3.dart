import 'package:flutter/material.dart';

class Story_3 extends StatelessWidget {
  const Story_3({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      backgroundColor: Colors.cyan,
    );
  }
}
